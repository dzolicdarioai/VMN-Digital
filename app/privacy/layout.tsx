import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Privacy Policy | VMN Digital',
  description:
    'Placeholder privacy policy for VMN Digital. Replace with counsel-approved content before production.',
}

export default function PrivacyLayout({ children }: { children: React.ReactNode }) {
  return children
}
